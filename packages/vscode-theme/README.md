# Rajzik Theme

Rajzik Theme is a dark Visual Studio Code theme packaged from this workspace.

## Local development

```sh
pnpm install
pnpm --filter rajzik-theme run package
```

To test the extension locally, launch the workspace from VS Code and use the `Launch Extension` configuration.
